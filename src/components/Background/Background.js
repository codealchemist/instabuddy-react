import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
  display: flex;
`

export default class Background extends React.PureComponent {
  render () {
    return <Wrapper>Background</Wrapper>
  }
}
